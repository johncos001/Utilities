Saving Jupyter notebooks to HTML

1) make sure all cells have been executed.
  (insert new cells to run functions if return values, graphs are needed as output)
2) from notebook:
  pull down File -> Make a Copy...
  (creates new webpage in browser)
2) from the new Copy of notebook in browser:
  pull down File -> Print Preview
  (creates new webpage in browser)
3) from the new Print Preview page in browser:
  pull down File -> Save Page as -> "web page, complete"
4) from finder:
  click to open html file and check that all code and graphic images are saved
  
  
5) from notebook:
  pull down Download as -> Notebook(.ipynp)
6) from notebook:
  pull down Download as -> Python(.py).
7) from sublime:
  open and save as script
